{"version":"9167e629ea493df9","fileName":"CyclicBarrier.java","fileSimpleName":"CyclicBarrier","fileType":"java","notes":{"260":"最后一个线程解锁，并且唤醒条件队列转到同步队列中的node","278":"指定线程数，副本，用于重置","214":"最后一个线程会进入此逻辑，不会进入条件队列，会将之前条件队列的node转成同步队列，方便后续唤醒","279":"真正用到的计数器，指定线程数","280":"线程都就绪后，执行的逻辑","200":"这里之所以使用独占锁，是因为 条件等待队列 condition，外层一定要是ReentrantLock","233":"条件队列，入队阻塞。相当于将同步队列转成条件队列","203":"generation表示完成一轮CyclicBarrier的记录","219":"如果构造器传入的barrierCommand不为null，执行barrierCommand中的逻辑","221":"开始下一代的内存屏障：\n1. 唤醒条件队列中的线程\n2. 重置线程数","222":"最后一个线程执行完finally中的解锁代码后，直接返回"}}